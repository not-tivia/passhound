//! Filled in by Task 5.

use crate::error::Result;
use crate::recovery::{Pool, RecoverConfig};
use crate::vault::Vault;

pub fn build(_vault: &Vault, _cfg: &RecoverConfig) -> Result<Pool> {
    unimplemented!("filled in by Task 5")
}
