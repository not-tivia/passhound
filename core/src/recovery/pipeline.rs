//! Filled in by Task 15.

use crate::error::Result;
use crate::recovery::{Candidate, RecoverConfig};
use crate::vault::Vault;

pub fn recover(_vault: &Vault, _config: RecoverConfig) -> Result<Vec<Candidate>> {
    unimplemented!("filled in by Task 15")
}
