{
  "identifier": "default",
  "description": "Default capability for the main window: core permissions + clipboard write + dialog open/save.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
