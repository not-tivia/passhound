{
  "identifier": "default",
  "description": "Default capability for the main window: clipboard write + dialog open/save. core:default removed — frontend only uses invoke (IPC, no core:* capability needed), not any event/window/path/app API.",
  "windows": ["main"],
  "permissions": [
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
